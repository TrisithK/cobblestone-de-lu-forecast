# Pinned dependencies — verified compatible set as of 2025-08
# Install with: pip install -r requirements.txt

# ENTSO-E client
entsoe-py==0.6.9

# Market data (TTF front-month)
yfinance==0.2.54

# Core data / ML
pandas==2.2.3
numpy==2.0.2
scikit-learn==1.5.2
lightgbm==4.5.0

# German public holidays
holidays==0.55

# Anthropic LLM
anthropic==0.40.0

# Schema validation for LLM output
pydantic==2.9.2

# Visualisation
matplotlib==3.9.2

# Secrets / env
python-dotenv==1.0.1

# Parquet I/O
pyarrow==17.0.0

# Markdown table rendering (pandas .to_markdown())
tabulate==0.9.0

# Transitive deps pinned for reproducibility
requests==2.32.3
lxml==5.3.0
pytz==2024.2

# [v2 round 2] XML parsing for data/fetch_ntc.py's custom NTC step-function
# parser — already a transitive dep of entsoe-py, pinned directly now that
# fetch_ntc.py imports it itself.
beautifulsoup4==4.15.0
